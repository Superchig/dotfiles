# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Inconsolata 13
# font pango:Ubuntu Mono 13

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioPlay exec playerctl play-play

# get color from XResource configuration - variable_name XResource fallback

# special
set_from_resource $foreground foreground #C6C6C6
set_from_resource $background background #1C1C1C
set_from_resource $cursorColor cursorColor #C6C6C6

# Black + DarkGrey
set_from_resource $black    color0 #303030
set_from_resource $darkgrey color8 #444444
set $trueblack #000000

# DarkRed + Red
set_from_resource $darkred color1 #d75f5f
set_from_resource $red     color9 #d75f5f

# DarkGreen + Green
set_from_resource $darkgreen color2 #87AF87
set_from_resource $green     color10 #87AF87

# DarkYellow + Yellow
set_from_resource $darkyellow color3 #ffd75f
set_from_resource $yellow     color11 #ffd75f

# DarkBlue + Blue
set_from_resource $darkblue color4 #87afd7
set_from_resource $blue     color12 #87afd7

# DarkMagenta + Magenta
set_from_resource $darkmagenta color5 #afafd7
set_from_resource $magenta     color13 #afafd7

# DarkGreen + Green
set_from_resource $darkgreen color6 #afd7ff
set_from_resource $green     color14 #afd7ff

# LightGrey + White
set_from_resource $lightgrey color7 #4E4E4E
set_from_resource $white     color15 #ffffff

set $fire_purple 9b1c39

# class                   border  backgr. text    indicator child_border
# client.focused            $magenta1   $magenta1   #FFFFFF $gyan2     $magenta1

## Default colors
# class                 border  backgr.   text    indicator child_border
client.focused          $darkgrey $darkblue #ffffff $yellow   $darkblue
# client.focused         #4c7899 #285577   #ffffff $yellow   $darkgrey
client.focused_inactive #333333   #5f676a   #ffffff #484e50   $darkgrey
client.unfocused        #333333   $black    $grey   #292d2e   $black
client.urgent           #2f343a   #900000   #ffffff #900000   #900000
client.placeholder      #000000   #0c0c0c   #ffffff #000000   #0c0c0c

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec st -e cd xcwd
# bindsym $mod+Return exec "cd $(xcwd); st"
bindsym $mod+Return exec $HOME/dotfiles/multi/portable-term

# kill focused window
# bindsym $mod+Shift+q kill
bindsym Control+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec rofi -show drun
bindsym $mod+q exec $HOME/dotfiles/minimalist/launch.sh
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# Rofi run terminal applications
bindsym $mod+Shift+q exec rofi -show run

# Rofi window selection
# bindsym Mod4+w exec rofi -show window
bindsym $mod+Shift+w exec rofi -show window

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# bindsym $mod+q split h

# split in vertical orientation
bindsym $mod+v split v
# bindsym $mod+Shift+v split v

# enter fullscreen mode for the focused container
# bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
# bindsym $mod+Shift+Control+d exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+Control+d exec $HOME/dotfiles/minimalist/rofi/i3_exit.sh

# bindsym $mod+Shift+b exec i3-msg border pixel 3
# bindsym --release $mod+Shift+b exec i3-msg border pixel 0
bindsym $mod+Shift+Control+p exec ~/dotfiles/minimalist/rofi/power_options.sh

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt; mode "default"
        bindsym j resize grow height 10 px or 10 ppt; mode "default"
        bindsym k resize shrink height 10 px or 10 ppt; mode "default"
        bindsym l resize grow width 10 px or 10 ppt; mode "default"

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt; mode "default"
        bindsym Down resize grow height 10 px or 10 ppt; mode "default"
        bindsym Up resize shrink height 10 px or 10 ppt; mode "default"
        bindsym Right resize grow width 10 px or 10 ppt; mode "default"

	# Moves the window to the center
	bindsym c move position center; mode "default"

	bindsym 1 resize set width 1920px; resize set height 1080px; mode "default"

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"; mode "default"
        bindsym Escape mode "default"; mode "default"
        bindsym $mod+r mode "default"; mode "default"
}

bindsym $mod+r mode "resize"

mode "launch" {
        bindsym f exec firefox; mode "default"
        bindsym n exec nautilus "$(xcwd)"; mode "default"
        bindsym c exec chromium; mode "default"
        bindsym z exec zoom; mode "default"
        # bindsym p exec firefox --private-window; mode "default"
        bindsym d exec "$HOME/dotfiles/scripts/electron-app.sh discord Discord"; mode "default"
        bindsym s exec "$HOME/dotfiles/scripts/electron-app.sh slack Slack"; mode "default"
        # bindsym b exec "$HOME/dotfiles/scripts/electron-app.sh bitwarden Bitwarden"; mode "default"
        bindsym b exec "$HOME/dotfiles/scripts/electron-app.sh blueman-manager Blueman-manager"; mode "default"

	# m for mail
	bindsym m exec "$HOME/dotfiles/scripts/electron-app.sh mailspring Mailspring"; mode "default"

 	# e for Emacs
	# bindsym e exec "$HOME/dotfiles/scripts/emacsclient-custom"; mode "default"
        bindsym e exec "$HOME/dotfiles/scripts/electron-app.sh emacs Doom Emacs"; mode "default"

        bindsym p exec pavucontrol; mode "default"
        bindsym o exec "$HOME/dotfiles/minimalist/rofi/rofi-open"; mode "default"
        bindsym q exec qalculate-gtk; mode "default"
        bindsym shift+q exec alacritty --title qalc -e qalc -s 'fractions off'; mode "default"

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+o mode "default"
}

bindsym $mod+o mode "launch"

# Assign first 4 workspaces to primary screen
workspace 1 output primary
workspace 2 output primary
workspace 3 output primary
workspace 4 output primary

# Start certain windows as floating
for_window [title="Watch It Player | Cengage Learning - Mozilla Firefox"] floating enable
for_window [title="Watch It - Mozilla Firefox"] floating enable
# for_window [title="Please Wait... - Mozilla Firefox"] floating enable
# for_window [title="Zotero Preferences"] floating enable
# for_window [title="Quick Format Citation"] floating enable
for_window [title="Sign in - Google Accounts - Mozilla Firefox*"] floating enable
for_window [title="Tab Style Selector - Mozilla Firefox"] floating enable
for_window [title="qtcreator_process_stub"] floating enable
# Make all Zotero windows float except the main one
for_window [title="Zotero"] floating disable
# For Steam friends list
for_window [title="Friends list"] floating enable
for_window [title="Steam - News*"] floating enable
for_window [title="SocialProgress"] floating enable
for_window [title="Quick Format Citation"] floating enable
for_window [title="Add/Edit Citation"] floating enable
for_window [title="Event Tester"] floating enable
for_window [title="Progress" class="Zotero"] floating enable
# for_window [class="Zotero"] floating enable
for_window [class="Org.gnome.Nautilus"] floating enable
for_window [class="Qalculate-gtk"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="vlc"] floating enable
# for_window [class="Gimp"] floating enable
for_window [class="Lxappearance"] floating enable
for_window [class="Arandr"] floating enable
for_window [class="System-config-printer.py"] floating enable
for_window [class="Sxiv"] floating enable
for_window [class="Xsane"] floating enable
for_window [class="yuzu"] floating enable
for_window [class="Gnome-screenshot"] floating enable
for_window [class="Alacritty" title="qalc"] floating enable
for_window [class="Alacritty" title="pulsemixer"] floating enable
for_window [class="Steam" title="Friends List"] floating enable
# for_window [class="mushclient.exe"] floating enable
for_window [class="OpenGL"] floating enable
for_window [class="openmw"] floating enable

# Show no border for new windows
default_border none

# You can also use any non-zero value if you'd like to have a border
for_window [class=".*"] border pixel 3
gaps inner 5
# workspace 2 gaps inner 0

# Only enable gaps on a workspace when there is at least one container
smart_gaps on

# Activate smart borders (always)
smart_borders on

# Start workspace 2 in tabbed mode
for_window [workspace=$ws2] layout tabbed
for_window [workspace=$ws4] layout tabbed

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
# 	# i3bar_command i3bar --transparency
#         position top
#         tray_output primary
#         status_command i3status

# 	font pango:Inconsolata 14

# 	colors {
# 		background $background
# 		statusline $foreground

# 		# colorclass      border      background   text
# 		focused_workspace $background $darkblue    $foreground
# 		binding_mode      $background $darkblue    $foreground
# 	}
# }

exec xset s off
exec xset -dpms
exec $HOME/bin/set-dpi
# Calls autorandr -c and other, more specific things if necessary
exec ~/dotfiles/multi/monitor_setup # Disabled while msi is docked
# exec autorandr -c
# exec ~/.fehbg
exec $HOME/dotfiles/minimalist/bar/launch_polybar.sh
exec picom --experimental-backends -b
exec /usr/lib/xfce4/notifyd/xfce4-notifyd
exec redshift &
exec alttab &

exec $HOME/dotfiles/multi/set-touchpad
# exec $HOME/dotfiles/multi/set-dpi # Disabled while msi is docked
# Turn on compose key, set to right alt
exec setxkbmap -option compose:ralt
# exec setxkbmap -option ctrl:swapcaps
# exec setxkbmap -option ctrl:nocaps

# Start more GUI-centric apps
exec $HOME/dotfiles/multi/startup-extra-apps

exec ~/dotfiles/scripts/polkit-agent

exec xscreensaver --no-splash &
